const score = 500  //at starting ye as a nummber hi declare hoga but we can change it also
console.log(score)

const balance =  new Number(100.236564)//we are changing it types as number
console.log(balance);

console.log(balance.toString().length);
console.log(balance.toFixed(2));

const otherNumber = 23.48484
console.log(otherNumber.toPrecision(3))